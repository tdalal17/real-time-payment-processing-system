# AWS SDK for Python (usually pre-installed in Lambda runtime)
boto3>=1.26.0
botocore>=1.29.0

# For JSON handling and data validation
requests>=2.28.0

# For decimal operations (built-in, but listed for clarity)
# decimal - built-in Python module

# For typing support (built-in Python 3.7+)
# typing - built-in Python module

# Development and testing dependencies (not needed in production)
# pytest>=7.0.0
# moto>=4.0.0  # For mocking AWS services in tests